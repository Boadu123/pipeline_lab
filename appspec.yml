# appspec.yaml
version: 0.0
Resources:
  - TargetService:
      Type: AWS::ECS::Service
      Properties:
        TaskDefinition: <arn:aws:ecs:eu-central-1:677276091734:task-definition/pipeline-lab-task:1>
        LoadBalancerInfo:
          ContainerName: "project-app"
          ContainerPort: 8080